# 更新日志

本项目按功能拆分小版本，每个版本对应一次可回退的提交（tag 形如 v0.1.0）。
下面是完整版本历史，最新版本排在最前。

## 0.14.0 — 侧边栏改用原生 source list
- 侧边栏从 SwiftUI 的 `List(selection:)` 换成 AppKit 的 `NSTableView`（`style = .sourceList`），
  选中态交给系统按 source list 的模糊材质绘制，并固定用中性那一种：「半透明覆盖层 + 彩色图标」，
  与最新版 App Store 一致；不再随焦点变成强调色实心胶囊。
- 尺寸全部取自原生控件，代码里不写任何尺寸常量：`rowSizeStyle` 用系统默认值，
  行高由表格自己更新，`NSTableCellView` 按标准度量摆放它的 `textField` / `imageView`
  （本机 `.large` 一档为 15pt 字号、40pt 行距、22×19 图标位），分组标题走系统的 group row 样式。
  因此字体大小仍跟随「辅助功能 → 文字大小」缩放。
- 新增 `LiquidGlassUI.SourceListSidebar`：泛型 `Value`、分组、SF Symbol 图标、数字徽标、
  选择同步（返回 / 前进切换到不在侧栏的页面时自动取消高亮），与业务无关，可复用。
- 两个坑记在这里：一是 `NSTableView` 的背景一旦被改成 `.clear` 之类，AppKit 就不再使用
  source list 的模糊材质，选中态退化成普通实心高亮；二是不要在 `layout()` 里替换图标，
  会打断 AppKit 当前的显示周期并抛异常崩溃。

## 0.13.0 — 图片统一九宫格
- 多图改回统一的九宫格：一律按方格裁切，2 / 4 张排两列、其余排三列，
  卡片高度只由行数决定，不再被横图（代码截图、网页长图）整幅撑开。
- 移除 0.8.0 引入的宽图整幅展示与宽高比测量：`FeedMediaGrid` 不再为排版预加载图片量尺寸，
  滚动时少一轮图片请求；随之无人使用的 `ImageStore.declaredAspect(of:)` 与 `aspect(for:)` 一并删除。
- 单图仍是按原比例自适应并限高，超长图保留「长图」标记。

## 0.12.0 — 文档与版本号同步
- README 补齐调试入口（`COOLAPK_TAB` / `COOLAPK_NOTIFY` / `COOLAPK_SCROLL` / `COOLAPK_DEBUG`），
  并说明沙盒内日志文件的位置与「推荐 / 头条」两套信息流的区别。
- 应用版本号同步到 0.11.0 → 本次改为 0.12.0，设置页「关于」显示的版本与 git tag 一致。
- 清掉 `DebugHooks` 的编译警告（`||` 短路结果未使用），clean build 无警告。

## 0.11.0 — 消息页玻璃化与统一交互
- 消息页的分类切换改用 `GlassPillBar` 胶囊玻璃控件，与首页 Tab 视觉统一（此前是系统分段控件）。
- 通知卡片与私信卡片补上 hover 抬升 + 手型光标反馈，和动态卡片保持一致的交互手感。

## 0.10.0 — 消息中心修复
- **修复消息列表整行空白**：五类通知字段名各不相同（系统通知 `from*`、@ 我 `user*`、赞 `like*`、
  私信 `messageUserInfo`），改为先判断通知种类再挑字段，头像、昵称、正文、时间都正常显示；
  正文用富文本渲染，链接可点。
- **修复点赞列表只剩一条**：点赞通知的 `id` 其实是动态 id，同一动态的多次点赞 key 相同，
  被 ForEach 去重成一条；现在用「动态 id + 点赞人 + 时间」组成唯一键。
- **修复点赞列表昵称头像错成动态作者**：优先取 `likeAvatar` / `likeUsername`。
- 新增动作短语（赞了你的动态 / 评论了你 / 提到了你 / 关注了你 / 系统通知），标题行不再空白。
- **请求取消不再报错**：加载统一走 `LoadError.message(_:)`，切页或视图重建时不会再弹出
  「未能完成操作。（Swift.CancellationError 错误 1。）」。
- 新增 `COOLAPK_NOTIFY=` 调试入口（直接打开消息页的某个分类），便于回归截图。

## 0.9.0 — 头条流接入与列表稳定性
- **头条不再与推荐混用**：新增「推荐」标签承载首页推荐流（`/v6/main/indexV8`），
  「头条」标签改用真正的头条接口 `/v6/main/headline`，默认进入「推荐」。
- **首屏空响应自动重试**：服务端偶尔对同一请求返回「成功但空」的 `{"data":[]}`（实测多次复现），
  此前会直接显示空页面；现在 600ms 后自动再取一次，实测两次复现都能恢复。
- **修复列表模型竞态**：首页 Tab 的模型改为同步创建（`onAppear` / `onChange`），列表按模型实例触发加载。
  此前同一个 Tab 会被并发建出两个模型，加载的是 A、界面渲染的是空的 B，日志里能看到「已加载 10 行」但页面空白。
- **修复加载中误报空**：新增 `hasLoaded`，首屏还没开始加载时显示加载态，不再闪出「这里还没有内容」。
- 竖图为主时格子按真实比例加高（比例下限 0.5），9:19.5 手机截图不再被正方形裁掉大半。
- 新增 `CoolapkLog` 注入点（`request` 日志）与 `COOLAPK_LOG_FILE`；应用开了沙盒，
  `/tmp` 写不进去，日志自动回落到容器临时目录，命令行可直接排查接口返回。
- `JSON` 增加 `isObject`，便于区分「列表为空」和「data 是对象」。

## 0.8.0 — 宽图整幅展示与版本号同步
- 列表图片网格：以横图为主的动态（代码截图、网页长图、桌面截图等）改为整幅纵向展示，
  不再被正方形格子裁成读不懂的画面；判定改用图片真实宽高比的**中位数**，
  超宽长条（≥ 2.5）不受张数限制，普通横图最多 6 张，避免横拍照片把卡片拉得过长。
- 宽高比优先从图床 URL 的 `@宽x高.扩展名` 后缀直接读出，读不到时才补一次真实加载
  （`ImageStore.declaredAspect(of:)` 与 `ImageStore.aspect(for:)`），列表滚动不额外发起请求。
- 版本号与 git tag 打通：`MARKETING_VERSION` 改为 `0.8.0`，设置页「关于」改为读取打包信息，不再硬编码 1.0。

## 0.7.0 — 崩溃修复与链接列表卡
- **修复崩溃**：`FeedHTML.attributedString` 中 `apply` 持有 `inout` 状态的同时回调 `append`，
  触发 Swift 独占访问冲突（SIGABRT）。改为 `apply` 只返回是否需要换行，写操作延后执行。
  该崩溃可在打开应用详情等任意富文本页面时随机触发。
- 移除 `<img>` 标签的同步网络加载（会阻塞主线程）。
- 新增纵向链接列表卡（`iconListCard` 内无配图时不再显示空占位图）。
- 调试入口支持内置页面：`COOLAPK_OPEN=nav:me` / `nav:settings` / `nav:notifications` / `nav:history` 等。

## 0.6.0 — 卡片模板补全与列表修复
- 修复首页切换 Tab 后新列表永不加载的问题（按数据源触发 `.task`，此前只有首个 Tab 有内容）。
- 修复动态详情评论区恒为空：`listType` 取值错误（`lastupdate` → `lastupdate_desc`），
  热门评论改用 `replyList?listType=popular`，因为 `/v6/feed/hotReplyList` 服务端已失效。
- 补全卡片模板渲染：分节标题、胶囊链接条、竖版子栏目（话题页子 Tab）、未登录引导、
  说明文字卡、横向商品（含京东联盟）、直播预告、图片按钮横幅。
- 新增实体类型 `pear_goods`（酷品）与 `liveTopic`（直播），连续同类自动合并成横滑。
- 榜单类卡片（`listCard` / `productTimelineListCard`）展开为商品行。
- 忽略页面元数据与广告卡片（`configCard` / `sponsorCard` 等），不再出现「默认配置（勿动）」。
- 请求取消不再弹出错误提示；新增 `COOLAPK_DEBUG=1` 请求日志与 `COOLAPK_TAB` / `COOLAPK_SCROLL` 调试入口。
- 浮动输入条改用「玻璃 + 画布垫层 + 底部渐隐」，评论输入框可读性明显提升。

## 0.5.0 — 布局与窗口修复
- 用户主页信息区重排版：用户名单行截断、简介自适应换行、等级条与关注按钮固定尺寸，
  解决长简介挤压按钮导致的错位。
- 用户模型兼容 `follow`/`follow_num` 两种字段命名（部分接口返回 snake_case）。
- 窗口最小尺寸下调至 940×640、默认 1408×872，小屏 Mac 上三栏不再被强制压缩。
- 侧边栏 / 内容列 / 详情列改为 min-ideal 弹性布局，拖拽分栏时排版稳定。

## 0.4.0 — 实用功能增强
- 浏览历史：工具栏前进 / 后退按钮，⌘[ 与 ⌘] 快捷键，最多保留 80 步。
- 窗口标题随当前页面变化（酷安 · 话题 / 用户主页 …）。
- 图片查看器：放大 / 缩小 / 还原、左右方向键翻页、上一张下一张按钮、
  一键设为桌面壁纸、存储到下载文件夹并自动在访达中显示。
- 发布动态：表情面板（本地 548 个官方表情，可搜索）、@用户 与 #话题# 搜索插入、
  剪贴板粘贴图片、拖拽图片文件到窗口。
- ⌘R 刷新当前列表，⇧⌘1 回到首页，首页 Tab 状态收归 AppStore 便于恢复。

## 0.3.0 — 模块化拆分
- 新增本地 Swift 包 `Packages/CoolapkKit`：签名请求、Token 生成、bcrypt、JSON、全部接口、
  数据模型、富文本渲染，不依赖任何 UI，可被其它项目直接引用。
- 新增本地 Swift 包 `Packages/LiquidGlassUI`：配色排版、玻璃组件、远程图片、图片查看器，
  与酷安业务解耦，可复用到任意 macOS 项目。
- 图片加载改为依赖注入：`ImageLoading.userAgent` / `ImageLoading.showsImages`，
  宿主 App 启动时注入，包内不再引用业务代码。
- 应用层只保留 `App/` 与 `Features/`，`project.yml` 通过本地包依赖引入两者。

## 0.2.0 — 液态玻璃 UI 升级
- 新增设计系统：统一配色（品牌蓝渐变、卡片、分隔线）、圆角与阴影规范。
- 玻璃组件库：`GlassPillBar` 胶囊分段控件、`GlassIconButton`、`GlassActionButton`、
  `GlassBar` 工具条、`GlassFooter`、`GlassTag`、hover 抬升效果。
- `AdaptiveRemoteImage` 按图片真实比例自适应高度，长图自动限高并标记。
- 外观设置：跟随系统 / 浅色 / 深色，全局生效并持久化。

## 0.1.0 — 基础完整版本
- 完整鉴权链路：Android UA + X-App-Token v2（自研 bcrypt，无第三方依赖），20 分钟缓存与失败自动重试。
- 首页信息流 /v6/main/indexV8：横幅轮播、图标网格、横滑卡片、动态卡片、商品/话题/用户/应用卡片。
- 首页 Tab（来自 /v6/main/init）：关注、头条、热榜、快讯、话题、新机、汽车、开箱、摄影、教程、视频等。
- 动态详情：正文富文本、表情、图片网格与图片查看器、话题标签、引用转发、目标卡片、点赞/收藏/评论/转发统计。
- 评论：热门/最新切换、楼中楼展开、点赞、回复、发送。
- 用户主页：动态/关注/粉丝/收藏、等级条、关注按钮。
- 搜索：动态/用户/话题三类、联想词、历史与热搜。
- 消息：评论、@我、@评论、赞、关注五类通知，私信列表，未读角标。
- 应用与游戏：列表、详情、截图、版本日志、评论区。
- 话题与商品页、浏览历史、我的收藏、发布动态（多图上传）、登录（WebView + Cookie 兜底）、设置。
- 深链路由：/t/、/u/、/feed/、/apk/、/product/、/collection/、/dyh/、/question/、/vote/、/page?url=
- 社交子页面：收藏夹、看看号、问答、投票讨论、点赞的人、转发列表、编辑历史。
- 图片与表情：548 个官方表情本地内置，图片内存 + 磁盘缓存与降采样。
